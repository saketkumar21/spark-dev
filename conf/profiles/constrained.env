# Constrained resource profile — used by `make up-constrained` for OOM / spill modules
# (small box so failure is real but the host stays usable).
SPARK_MEM_LIMIT=2g
SPARK_DRIVER_MEMORY=1g
SPARK_CORES=2

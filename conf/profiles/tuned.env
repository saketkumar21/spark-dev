# Tuned resource profile — used by `make up` (the default). SINGLE SOURCE OF TRUTH for
# the spark-connect container's memory/cores. Constrained box: conf/profiles/constrained.env
SPARK_MEM_LIMIT=3g
SPARK_DRIVER_MEMORY=2g
SPARK_CORES=*

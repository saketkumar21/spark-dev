# Auto-provisions the Prometheus datasource so Grafana is usable the moment it starts.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

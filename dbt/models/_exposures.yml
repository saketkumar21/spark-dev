version: 2
exposures:
  - name: orders_dashboard
    type: dashboard
    maturity: high
    url: http://localhost:8080
    description: "Illustrative executive orders dashboard — shows up in `dbt docs` lineage."
    depends_on:
      - ref('fct_orders')
      - ref('dim_customers')
    owner:
      name: Data Team
      email: data@example.com
